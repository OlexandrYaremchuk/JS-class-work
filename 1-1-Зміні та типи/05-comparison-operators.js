/*
 * Оператори порівняння
 */

// console.log(15 < 3);

/*
 * Приведення типів до числа
 */

// console.log('10' > 5);

/*
 * Оператори рівності (== і ===) і нерівності (!= і !==)
 * ✅ «ЗАВЖДИ ВИКОРИСТОВУЄМО СТРОГУ РІВНІСТЬ (===) ТА НЕРІВНІСТЬ (!==)
 */

const isEqual = 1 === true;

console.log(isEqual);
